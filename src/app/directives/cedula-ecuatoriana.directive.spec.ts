import { CedulaEcuatorianaDirective } from './cedula-ecuatoriana.directive';

describe('CedulaEcuatorianaDirective', () => {
  it('should create an instance', () => {
    const directive = new CedulaEcuatorianaDirective();
    expect(directive).toBeTruthy();
  });
});
